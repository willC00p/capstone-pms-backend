## Dockerfile intentionally disabled in repository. Use local Dockerfile if you
## want to run a face-match microservice. This placeholder prevents accidental
## builds in CI or local scripts.
FROM scratch
CMD ["/bin/sh", "-c", "echo 'face-match-server disabled' && sleep 3600"]
